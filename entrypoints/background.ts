// Placeholder — becomes the messaging/offscreen/context-menu/commands hub
// ported from background/background.js + translationService.js +
// translationCache.js + textToSpeech.js in later phases (see plan Phase 1+).
export default defineBackground(() => {
  console.log('[TWP] background placeholder loaded', { id: browser.runtime.id });
});
